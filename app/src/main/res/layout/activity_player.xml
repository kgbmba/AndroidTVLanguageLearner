<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- ExoPlayer View -->
    <com.google.android.exoplayer2.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        app:controller_layout_id="@layout/exo_player_control_view"
        app:show_subtitle_button="true"
        app:show_vr_button="false" />

    <!-- Stream Title -->
    <TextView
        android:id="@+id/streamTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:marqueeRepeatLimit="marquee_forever"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:scrollHorizontally="true"
        android:text="正在加载..." />

    <!-- Subtitle Display -->
    <TextView
        android:id="@+id/subtitleTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="80dp"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@drawable/subtitle_background"
        android:padding="12dp"
        android:lineSpacingMultiplier="1.2"
        android:maxLines="3"
        android:ellipsize="end"
        android:text="字幕加载中..." />

    <!-- Learning Tools Overlay -->
    <LinearLayout
        android:id="@+id/learningToolsLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/subtitleTextView"
        android:layout_marginBottom="20dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:visibility="gone">

        <Button
            android:id="@+id/btnRepeat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="重复"
            android:textSize="14sp"
            android:padding="10dp"
            android:layout_margin="5dp"
            android:background="@drawable/button_background" />

        <Button
            android:id="@+id/btnSlowSpeed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="慢速"
            android:textSize="14sp"
            android:padding="10dp"
            android:layout_margin="5dp"
            android:background="@drawable/button_background" />

        <Button
            android:id="@+id/btnTranslate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="翻译"
            android:textSize="14sp"
            android:padding="10dp"
            android:layout_margin="5dp"
            android:background="@drawable/button_background" />

        <Button
            android:id="@+id/btnDictionary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="词典"
            android:textSize="14sp"
            android:padding="10dp"
            android:layout_margin="5dp"
            android:background="@drawable/button_background" />

    </LinearLayout>

    <!-- Word Highlight Overlay -->
    <TextView
        android:id="@+id/wordHighlightTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="200dp"
        android:textColor="#FFFF00"
        android:textSize="32sp"
        android:textStyle="bold"
        android:background="@drawable/word_highlight_background"
        android:padding="15dp"
        android:visibility="gone" />

</RelativeLayout>